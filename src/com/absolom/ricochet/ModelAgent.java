package com.absolom.ricochet;

import com.absolom.ricochet.model.Game;
import com.absolom.utility.messaging.MessageTransceiver;

public class ModelAgent {
	// private Game m_game;

	public ModelAgent(MessageTransceiver messageTransceiver, Game game) {
		// m_game = game;
		// registerMessageReceiver(GetCurrentStateCommand.class,
		// onGetCurrentStateCommand);

	}

	/*
	 * private IMessageReceiver onGetCurrentStateCommand = new
	 * GetCommandHandler() { public MessageResult onCommandReceived(ICommand
	 * command) { return null; //new
	 * GetCurrentStateCommand.Result(m_currentState.getType()); } };
	 */

	/*
	 * private Game getGame() { return m_game; }
	 * 
	 * private ModelAgent getInstance() { return this; }
	 */

}
